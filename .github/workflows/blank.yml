name: Extract ReadXHub

on:
  workflow_dispatch:

jobs:
  extract:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Extract ZIP
        run: unzip "ReadXHub-v2-UPDATED.zip" -d extracted

      - name: Show extracted files
        run: ls -R extracted
